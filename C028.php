<?php
    // 自分の得意な言語で
    // Let's チャレンジ！！
    $d = fgets(STDIN);
    $l = explode(" ", $d);
    $M=$l[0];
    $N=$l[1];
    for($i=0;$i<$M;$i++){
        $M_[$i]=fgets(STDIN);
        $x = explode(" ", $M_[$i]);
        $d_[$i]=$x[0];
        $r_[$i]=$x[1];
    }
    for($d_[$i])
?>
